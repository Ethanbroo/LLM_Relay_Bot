"""User-created custom templates.

Place custom template modules here and register them via the template registry:

    from connectors.video_pipeline.templates.registry import register_template
    register_template(MyCustomTemplate())
"""
